<script>
    $(function() {
        {% include 'PolitizrFrontBundle:JS:_sticky.js.twig' %}
    });

    // Allez à ancre <date>
    $("body").on("change", "[action='goToDate']", function(e) {
        console.log('*** goToDate');
        console.log($('option:selected').val());
        location.hash = $('option:selected').val();
    });
</script>
