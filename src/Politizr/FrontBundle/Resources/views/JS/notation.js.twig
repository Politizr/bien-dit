<script>
    // **************************** //
    //      AJAX
    // **************************** //

    // Noter
    $("body").on("click", "[action='note']", function(e) {
        console.log('*** click note');
        
        e.preventDefault();

        $.ajax({
            type: 'POST',
            url: "{{ path('Note') }}",
            context: $(this).closest('.notes'),
            data: { 'objectId': $(this).attr('object-id'), 'type': $(this).attr('type'), 'way': $(this).attr('way') },
            dataType: 'json',
            {% include 'PolitizrFrontBundle:JS:ajaxError.js.twig' %}
            success: function(data) {
                $('#ajaxGlobalLoader').hide();
                if (data['error']) {
                    $('#infoBoxHolder.boxError.notifBoxText').html(data['error']);
                    $('#infoBoxHolder.boxError').show();
                } else {
                    $(this).html(data['html']);
                }
            }
        });

    });


</script>
