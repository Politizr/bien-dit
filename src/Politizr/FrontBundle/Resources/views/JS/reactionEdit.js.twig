<script>
    // **************************** //
    //      Initialisation editeur
    // **************************** //
    var summaryEditor = new MediumEditor('.summary-editable', {
        // buttonLabels: 'fontawesome',
        anchorButtonClass: 'mdm-btn',
    });
    var descriptionEditor = new MediumEditor('.description-editable', {
        // buttonLabels: 'fontawesome'
        anchorButtonClass: 'mdm-btn',
    });
    var moreInfoEditor = new MediumEditor('.moreInfo-editable', {
        // buttonLabels: 'fontawesome'
        anchorButtonClass: 'mdm-btn',
    });

    // **************************** //
    //      POST
    // **************************** //

    // Update reaction
    $('body').on('submit', '#form-reaction-update', function(e){
        console.log('*** click submit');

        e.preventDefault();

        var summary = summaryEditor.serialize();
        var description = descriptionEditor.serialize();
        var moreInfo = moreInfoEditor.serialize();

        console.log(summary['element-0']['value']);
        console.log(description['element-0']['value']);
        console.log(moreInfo['element-0']['value']);

        $('#reaction_summary').val(summary['element-0']['value']);
        $('#reaction_description').val(description['element-0']['value']);
        $('#reaction_more_info').val(moreInfo['element-0']['value']);
        
        $.ajax({
            type: 'POST',
            dataType: 'json',
            url : "{{ path('ReactionUpdate') }}",
            data: $("#form-reaction-update").serialize(),
            dataType: 'json',
            {% include 'PolitizrFrontBundle:JS:ajaxError.js.twig' %}
            success: function(data) {
                $('#ajax-loader').hide();
                if (data['error']) {
                    $('.alert-danger-msg').html(data['error']);
                    $('.alert.alert-danger').show();
                } else {
                    $('.alert-success-msg').html('Objet bien enregistré!');
                    $('.alert.alert-success').show();
                }
            }
        });

        return false;
    });

    // Retour liste
    $('body').on('click', "a[action='reaction-list']", function(e){
        console.log('*** click list');

        bootbox.confirm('Pensez à enregistrer votre travail avant de revenir à la liste. Voulez-vous revenir à la liste?', function(result) {
            if (result) {
                window.location = $("a[action='reaction-list']").attr('url');
            }
        });
    });

    // Prévisualisation
    $('body').on('click', "a[action='reaction-preview']", function(e){
        console.log('*** click preview');

        bootbox.confirm('Pensez à enregistrer votre travail avant de prévisualiser votre réaction. Voulez-vous prévisualiser votre réaction?', function(result) {
            if (result) {
                window.location = "{{ path('ReactionDraftPreview', { 'id': reaction.id }) }}";
            }
        });
    });


</script>
