<div id="scrollNav">
    {% if moreResults %}
        <a id="moreResults" action="{{ action }}" offset="{{ offset }}">suite</a>
        {#<div id="moreResults" class="waypoint" action="{{ action }}" offset="{{ offset }}"></div>#}
    {% endif %}
</div>