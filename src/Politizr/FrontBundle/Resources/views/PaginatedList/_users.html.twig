{# beta #}
{% for user in users %}
    {% include 'PolitizrFrontBundle:User:_card.html.twig' with { 'user': user } %}
{% endfor %}
{% if app.user %}
    <div id="listingScrollNav" class="scrollNav">
        {% if moreResults %}
            <a id="moreResults" action="listingNext" class="waypoint" offset="{{ offset }}"{% if uuid is defined %} uuid="{{ uuid }}"{% endif %} key="{{ jsFunctionKey }}">suite</a>
        {% endif %}
    </div>
{% else %}
    <a class="linkCreateAccount" href="{{ path('InscriptionChoice') }}"><i class="icon-alert"></i>Créer un compte pour voir plus de résultats</a>
{% endif %}