{% set homePath = path('Homepage') %}
{% if app.user %}
    {% set homePath = path('Homepage'~profileSuffix()) %}
{% endif %}
<div id="logo"><a href="{{ homePath }}"><img src="{{ asset('uploads/default_logo.jpg') }}"/></a></div>

