{# beta #}
<div class="statTime"><i class="icon-time"></i>{{ document | readingTime }}</div>
<div class="statViews"><i class="icon-graph"></i>{{ document | nbViews }}</div>
<div class="statComments"><i class="icon-comment"></i>{{ document | nbComments }}</div>
{% if facebook %}
    <div class="statFB withLoaderInside" uuid="{{ document.uuid }}" type="{{ document.type }}">
        {% include 'PolitizrFrontBundle:Navigation\\Xhr:_xhrLocalLoader.html.twig' %}
    </div>
{% elseif document.fbAdId %}
    <div class="statFB"><i class="icon-social-fb"></i>Statistiques Facebook disponibles dans le détail</div>
{% endif %}
<div class="statLoc">{{ document | localizations }}</div>