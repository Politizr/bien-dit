<div class="scroll">
    {% for timelineRow in timeline %}
        {{ timelineRow(timelineRow) }}
        <hr>
    {% endfor %}
</div>
{% include 'PolitizrFrontBundle:Fragment\\Global:ScrollNav.html.twig' with { 'results': timeline, 'offset': offset } %}

<script>
    $(document).ready(function () {
        // Camouflage des détails
        $(".jumbotron").hide();
    });
</script>