{# beta #}
<div class="sidebarAskForModification askForUpdateBox">
    <div class="askForModificationButton" action="askForUpdate" uuid="{{ subject.uuid }}" type="{{ subject.type }}">
        <i class="icon-publish"></i>
        <span>Demande de modification</span>
    </div>
    <div class="sidebarAskForModificationForm askForUpdateForm withLoaderInside" style="display: none">  
        {% include 'PolitizrFrontBundle:Navigation\\Xhr:_xhrLocalLoader.html.twig' %}
        <div class="sidebarNote">
            Ces informations ne peuvent normalement pas être modifiées, merci de justifier votre demande.
        </div>
        <div class="formAskForUpdateContent">
        </div>
    </div>
</div>
