{% include 'PolitizrFrontBundle:Navigation:_ajaxLocalLoader.html.twig' %}
{% if app.user %}
    {% if follower %}
        <a action="followDebate" object-id="{{ object.id }}" way="unfollow">
            <i class="iconCross"></i>
            Se désabonner
        </a>
    {% else %}
        <a action="followDebate" object-id="{{ object.id }}" way="follow">
            <i class="iconCheckInSquare"></i>
            S'abonner
        </a>
    {% endif %}
{% else %}
    <a href="{{ path('Inscription') }}">S'abonner</a>
{% endif %}
