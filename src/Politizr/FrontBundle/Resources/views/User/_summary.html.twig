{# beta #}
<div class="miniProfile">
    {% if user %}
        <div class="avatar40">{{ user | photo('user_40') }}</div>
        <div class="profilenInfo">
            {% include 'PolitizrFrontBundle:User:_author.html.twig' with { 'user': user } only %}
            {% include 'PolitizrFrontBundle:User:_biography.html.twig' with { 'user': user } only %}
        </div>
        {% if withSocialLinks is defined and withSocialLinks %}
            {% include 'PolitizrFrontBundle:User:_socialLinks.html.twig' with { 'user': user } only %}
        {% endif %}
    {% else %}
        <div class="deletedAccount">utilisateur supprimé</div>
    {% endif %}
    {% if user and user.qualified and withMandates is defined and withMandates and mandate.active %}
        <div class="profileMandats">
            {% include 'PolitizrFrontBundle:User:_mandates.html.twig' with { 'user': user, 'mandates': user.mandates } only %}
        </div>
    {% endif %}
</div>
