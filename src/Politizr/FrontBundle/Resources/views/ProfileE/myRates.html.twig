{% extends 'PolitizrFrontBundle::layoutE.html.twig' %}

{% block main %}
	<div id="main" class="container">
		<h3>Mes brouillons</h3>
		<h4>Mes débats</h4>
        {% for draft in debateDrafts %}
			<div class="table-responsive">
				<table class="table table-hover">
					<thead>
				        <tr>
				            <th>Bandeau</th>
				            <th>Titre</th>
				            <th>Résumé</th>
				            <th>Date de création</th>
				            <th>&nbsp;</th>
				        </tr>
			      	</thead>				
			        {% for draft in drafts %}
						{% include 'PolitizrFrontBundle:Fragment\\Debate:DraftTableRow.html.twig' with { 'document': draft } %}
					{% else %}
						<tr>
							<td colspan="5">
								Aucun brouillon rédigé pour le moment
							</td>
						</tr>
					{% endfor %}
				</table>
			</div>
			<a class="btn btn-primary" href="{{ path('DebateDraftNew') }}">Nouveau</a>
		{% else %}
			<div class="no-results">
				<a class="btn btn-primary" href="{{ path('MyDraftsC') }}">Vous n'avez rédigé aucun brouillon de débat pour le moment</a>
			</div>
		{% endfor %}
		<h4>Mes réactions</h4>
        {% for draft in reactionDrafts %}
			<div class="table-responsive">
				<table class="table table-hover">
					<thead>
				        <tr>
				            <th>Bandeau</th>
				            <th>Titre</th>
				            <th>Résumé</th>
				            <th>Date de création</th>
				            <th>&nbsp;</th>
				        </tr>
			      	</thead>				
			        {% for draft in drafts %}
						{% include 'PolitizrFrontBundle:Fragment\\Debate:DraftTableRow.html.twig' with { 'document': draft } %}
					{% else %}
						<tr>
							<td colspan="5">
								Aucun brouillon rédigé pour le moment
							</td>
						</tr>
					{% endfor %}
				</table>
			</div>
		{% else %}
			<div class="no-results">
				<a class="btn btn-primary" href="{{ path('MyDraftsC') }}">Vous n'avez rédigé aucun brouillon de réaction pour le moment</a>
			</div>
		{% endfor %}
		<h3>Mes débats publiés</h3>
        {% for debate in debates %}
			{% include 'PolitizrFrontBundle:Fragment\\Debate:ListRow.html.twig' with { 'debate': debate } %}
			<hr>
		{% else %}
			<div class="no-results">
				<a class="btn btn-primary" href="{{ path('MyDraftsE') }}">Vous n'avez aucun débat en ligne pour le moment</a>
			</div>
		{% endfor %}
		<h3>Mes réactions publiés</h3>
        {% for reaction in reactions %}
			{% include 'PolitizrFrontBundle:Fragment\\Reaction:ListRow.html.twig' with { 'reaction': reaction } %}
			<hr>
		{% else %}
			<div class="no-results">
				<a class="btn btn-primary" href="{{ path('MyDraftsE') }}">Vous n'avez aucune réaction en ligne pour le moment</a>
			</div>
		{% endfor %}
		<h3>Mes commentaires</h3>
        {% for comment in comments %}
			{% include 'PolitizrFrontBundle:Fragment\\Comment:ListRow.html.twig' with { 'comment': comment } %}
			<hr>
		{% else %}
			<div class="no-results">
				<a class="btn btn-primary" href="{{ path('TimelineE') }}">Vous n'avez rédigé aucun commentaire pour le moment</a>
			</div>
		{% endfor %}
	</div>
{% endblock main %}

{% block javascriptFooter %}
	{{ parent() }}
{% endblock javascriptFooter %}
