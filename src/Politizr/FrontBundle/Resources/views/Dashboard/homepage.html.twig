{% extends 'PolitizrFrontBundle::layoutConnected.html.twig' %}

{% block title %}
    {{ parent() }} - Tableau de bord
{% endblock title %}

{% block main %}
    <div id="main">
        <h1>Carte de France</h1>
        <h2>Liste des régions</h2>
        <div>
            {% for regionTag in regionTags %}
                <a action="modalTagged" model="PTag" slug="{{ regionTag.slug }}">{{ regionTag.title }}</a>
            {% endfor %}
        </div>
    </div>
{% endblock main %}
