{# beta #}
{% if author %}
    {% if not (
            notificationId == constant('Politizr\\Constant\\NotificationConstants::ID_D_NOTE_NEG')
            or notificationId == constant('Politizr\\Constant\\NotificationConstants::ID_D_C_NOTE_NEG')
        ) %}
        {% include 'PolitizrFrontBundle:Notification\\Message:_screenUserPhoto.html.twig' with {'user': author} only %}
    {% endif %}
{% endif %}
<div class="notifItemHeader">
    {{ notification.createdAt | localizeddate('medium', 'short', app.request.locale, "Europe/Paris", "EEEE d MMMM à HH'h'mm" ) | capitalize }}
    {% if not notification.checked %}
        <i action="notificationCheck" class="icon-check-incircle" uuid="{{ notification.uuid }}"></i>
    {% endif %}
</div>
<div class="notifItemContent">
    <span {% if not notification.checked %}class="notifHighlight"{% endif %} uuid="{{ notification.uuid }}">
        {% include 'PolitizrFrontBundle:Notification:_notificationMessage.html.twig' %}
    </span>
</div>
