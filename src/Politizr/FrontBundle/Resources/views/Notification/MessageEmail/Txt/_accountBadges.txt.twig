{% spaceless %}
Félicitations!
{% if badges|length > 1 %}
    Vous avez obtenu les badges
    {% for badge in badges %}
        "{{ badge['title'] }}"
        {% if loop.revindex == 2 %}
            et
        {% elseif not loop.last %}
            ,
        {% else %}
            .
        {% endif %}
    {% endfor %}
{% else %}
    Vous avez obtenu le badge "{{ badges[0]['title'] }}"
{% endif %}
{% endspaceless %}
