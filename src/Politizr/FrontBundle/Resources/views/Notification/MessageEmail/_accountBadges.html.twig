<tr style="background-color: #FFFFFF; width: 100%; padding-bottom: 20px;">
    <td style="padding: 0 40px 0 40px; line-height: 140%;" valign="top">
        {% spaceless %}
        Félicitations!
        {% if badges|length > 1 %}
            Vous avez obtenu les badges
            {% for badge in badges %}
                "{{ badge['title'] }}"
                {% if loop.revindex == 2 %}
                    et
                {% elseif not loop.last %}
                    ,
                {% else %}
                    .
                {% endif %}
            {% endfor %}
        {% else %}
            Vous avez obtenu le badge "{{ badges[0]['title'] }}"
        {% endif %}
        {% endspaceless %}
    </td>
</tr>