<tr style="background-color: #FFFFFF; width: 100%;">
    <td colspan="2" style="float:left; width: calc(100% - 80px); padding: 40px 0; margin: 0 40px; line-height: 180%; border-bottom: solid 1px #2D2D2D;">
        {% spaceless %}
        {% if debates|length > 1 %}
            <h1>De nouveaux sujets qui pourraient vous intéresser!</h1>
        {% else %}
            <h1>Un nouveau sujet qui pourrait vous intéresser!</h1>
        {% endif %}
        {% for debate in debates %}
            {% set author = debate.user %}
            "<a href="{{ url('DebateDetail', {'slug': debate.slug}) }}" style="color: #2D2D2D">{{ debate.title }}</a>" {% if author %} par <a href="{{ url('UserDetail', {'slug': author.slug}) }}" style="color: #2D2D2D">{{ author.fullName }}</a>{% endif %}, localisé sur {{ debate | localizations(inMail=true) }}.
            <br/>
        {% endfor %}
        {% endspaceless %}
        <br/>
        <a href="{% if user.qualified %}{{ url('HomepageE') }}{% else %}{{ url('HomepageC') }}{% endif %}" style="color: #2D2D2D">Voir mon fil d'actualité</a> - <a href="http://politizr.help/article/suivre-une-discussion-et-enrichir-son-fil-dactualite/" style="color: #2D2D2D">Comment suivre l'actualité de nouveaux sujets?</a>
    </td>
</tr>