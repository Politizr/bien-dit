################################################################################
#           NOTIFICATIONS
################################################################################
NotificationsLoad:
    pattern:  /profil/load-notif.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notificationsLoad, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationCheck:
    pattern:  /profil/check-notif.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notificationCheck, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationsCheckAll:
    pattern:  /profil/check-all-notif.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notificationsCheckAll, _format: json }
    requirements: { _format: (xml|json), _method: POST }

################################################################################
#           GESTION INFOS SOUSCRIPTION NOTIFICATIONS
################################################################################
NotificationEmailSubscribe:
    pattern:  /profil/email-subscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifEmailSubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationEmailUnsubscribe:
    pattern:  /profil/email-unsubscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifEmailUnsubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationUserContextSubscribe:
    pattern:  /profil/user/context-subscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifUserContextSubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationUserContextUnsubscribe:
    pattern:  /profil/user/context-unsubscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifUserContextUnsubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationDebateContextSubscribe:
    pattern:  /profil/debate/context-subscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifDebateContextSubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }

NotificationDebateContextUnsubscribe:
    pattern:  /profil/debate/context-unsubscribe.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrNotification:notifDebateContextUnsubscribe, _format: json }
    requirements: { _format: (xml|json), _method: POST }
