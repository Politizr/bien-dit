################################################################################
#               ROUTING XHR SUIVI DEBAT / USER
################################################################################
FollowUser:
    pattern:  /profil/follow.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrFollow:followUser, _format: json }
    requirements: { _format: (xml|json), _method: POST }

FollowDebate:
    pattern:  /debat/follow.{_format}
    defaults: { _controller: PolitizrFrontBundle:Xhr\XhrFollow:followDebate, _format: json }
    requirements: { _format: (xml|json), _method: POST }

