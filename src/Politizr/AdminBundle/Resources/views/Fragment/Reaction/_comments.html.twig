<div id="displayCommentsZone">
    <a href="{{ path('Politizr_AdminBundle_PDRComment_list') }}">
        {% if comments | length > 0 %}
            <b>{{ comments | length }} commentaires ont été rédigés pour cette réaction.</b>
        {% else %}
            <b>Aucun commentaire rédigé pour cette réaction.</b>
        {% endif %}
    </a>
    <hr>
    {% for comment in comments %}
        {% include 'PolitizrAdminBundle:Fragment\\Comment:_comment.html.twig' with { 'comment': comment, 'withDocContext': false } %}
    {% endfor %}
</div>
