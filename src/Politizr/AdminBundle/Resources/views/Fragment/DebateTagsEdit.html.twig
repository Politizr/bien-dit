<div id="tagsZone-{{ zoneId }}">
    {# Variables #}
    <div class="tagsAttr" ptTagTypeId="{{ ptTagTypeId }}" objectId="{{ pdDebate.id }}" addUrl="{{ path('AdminAddPDDTaggedTPTag') }}"></div>
    
    {# Sélection #}
    <input type="text" class="selectedTag" />
    <input type="hidden" class="selectedTagID" />
    <a class="addUserTag">Ajouter</a>

    {# Affichage #}
    <div class="displayTagsZone">
        {% for pTag in pTags %}
            {% include 'PolitizrAdminBundle:Fragment:Tag.html.twig' with { 'objectId': pdDebate.id, 'pTagId': pTag.id, 'pTagTitle': pTag.title, 'deleteUrl': path('AdminDeletePDDTaggedTPTag') } %}
        {% endfor %}
    </div>
</div>
