<div class="addTag" id="editTagZone-{{ zoneId }}" tagTypeId="{{ tagTypeId }}" uuid="{{ object.uuid }}" newTag="{{ newTag }}" path="{{ pathCreate }}">

    <div class="editableTagList">
        {% for tag in tags %}
            {% include 'PolitizrAdminBundle:Fragment\\Tag:_detailEditable.html.twig' with { 'uuid': object.uuid, 'tag': tag, 'path': pathDelete } %}&nbsp;
        {% endfor %}
    </div>

    <hr/>
    
    {# Sélection #}
    <input type="text" class="selectedTag" placeholder="tag" />
    <input type="hidden" class="selectedTagUuid" />
    <button type="button" action="addTag">Ajouter</button>
</div>
