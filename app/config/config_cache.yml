imports:
    - { resource: config.yml }


# app/config/config.yml
fos_http_cache:
    proxy_client:
        symfony:
            http:
                servers: [ 127.0.0.1 ]
                base_url: %http_cache_base_url_dev%

    user_context:
        enabled: true
        role_provider: true
